# AppIconPicker

Icon picker for a searchable set of Font Awesome icons. Used where a user must pick a single icon (e.g. map marker style in callout builder). Search filters by name; click an icon to select it.

**Props:** `iconStyle` (solid, regular, light, duotone, brands), `initialSearchQuery` (default search text), `limit` (max icons in grid).
