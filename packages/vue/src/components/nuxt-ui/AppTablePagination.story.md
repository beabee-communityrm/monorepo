# AppTablePagination

Pagination footer shared by paginated tables and lists, so they stay
consistent.

`page` is zero-based, matching the API's offset-based queries. Pass
`limitOptions` for tables where the user can change the page size, or
`pageSize` for lists with a fixed one.
