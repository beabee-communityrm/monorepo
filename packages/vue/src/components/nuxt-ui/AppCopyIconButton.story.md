# AppCopyIconButton

An icon-only copy-to-clipboard button for Nuxt UI inputs/fields (e.g. a
`UInput`'s trailing slot). Swaps to a checkmark and `success` colour for
1.5s after a successful copy, and shows an error notification if the
clipboard write fails.
