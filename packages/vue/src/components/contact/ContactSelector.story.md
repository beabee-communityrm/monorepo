# ContactSelector

Compact contact selector with prev/next buttons and fixed-width display of the current contact. No built-in label; the parent renders any label above the control. Uses `contactSelector.contactNOfTotal`, `contactSelector.selfOption`, and `actions.previous` / `actions.next` for i18n.
