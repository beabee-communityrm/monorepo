/**
 * Trigger types for segment ongoing emails.
 * - onJoin: Send email when a contact joins the segment
 * - onLeave: Send email when a contact leaves the segment
 */
export type SegmentOngoingEmailTrigger = 'onJoin' | 'onLeave';
