import type { CalloutComponentBaseRules } from "./index.ts";

export interface CalloutComponentSelectableRules
  extends CalloutComponentBaseRules {
  /** TODO: Add description. */
  onlyAvailableItems?: boolean;
}
