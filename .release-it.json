{
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}",
    "requireBranch": "main",
    "requireCleanWorkingDir": true
  },
  "github": {
    "release": true,
    "releaseName": "v${version}",
    "autoGenerate": true
  },
  "npm": {
    "publish": false
  },
  "hooks": {
    "before:init": ["yarn check", "yarn build"],
    "before:bump": "node scripts/update-changelog.mjs ${version} ${latestVersion}"
  },
  "plugins": {
    "@release-it/bumper": {
      "out": ["package.json", "packages/*/package.json", "apps/*/package.json"]
    }
  }
}
