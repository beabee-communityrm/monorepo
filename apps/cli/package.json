{
  "name": "@beabee/cli",
  "version": "0.16.0",
  "main": "cli",
  "type": "module",
  "files": [
    "src"
  ],
  "bin": {
    "beabee-cli": "./cli.bin",
    "beabee-cli-node": "./cli.js",
    "beabee-cli-sh": "./cli.sh"
  },
  "scripts": {
    "beabee:cli": "yarn exec 'deno task beabee:cli'",
    "build": "yarn exec 'deno task build'",
    "build:compile": "yarn exec 'deno task build:compile'",
    "build:install": "yarn exec 'deno task build:install'",
    "generate:index": "yarn exec 'deno task generate:index'",
    "sync": "yarn exec 'deno task sync'",
    "deno:cache": "yarn exec 'deno task deno:cache'",
    "deno:cache:refresh": "yarn exec 'deno task deno:cache:refresh'",
    "chmod": "yarn exec 'deno task chmod'"
  },
  "dependencies": {}
}
